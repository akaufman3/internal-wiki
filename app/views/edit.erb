<section id="main" class="articleContainer">
	<% @article_info.each do |data| %>
		<form class="edit" action ="/edit" method="post">
			<div class="input-row">
				<input type="submit" value="Save" />
			</div>

			<div class="input-row">
				<label for="category">Category: </label>
				<select name="category">
					<% @categories.each do |category| %>
						<% if category['category'] == data['category'] %>
							<option value="<%= data['category'] %>" selected><%= data['category'] %></option>
						<% elsif category['category'] != data['category'] %>
							<option value="<%= "#{category['category']}" %>"><%= "#{category['category']}" %></option>
						<% end %>
					<% end %>
				</select>
			</div>

			<div class="input-row">
				<label for="title">Title: </label>
				<input type="text" name="title" value="<%= data['title'] %>"/>
			</div>

			<div class="input-row">
				<label for="author">Author: </label>
				<input type="text" name="author" value="<%= data['author'] %>"/>
			</div>

			<div class="input-row">
				<label for="copy">Copy: </label>
				<textarea type="text" name="copy"><%= data['copy'] %></textarea>
			</div>
		</form>
	<% end %>
	<div class="clearfix"></div>
</section>
